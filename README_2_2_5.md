# AveliCoach 2.2.5

Correções no PDF/HTML exportado:
- coluna Ref. do PDF usa `min.` e `max.` para evitar caracteres quebrados em ≥/≤;
- cabeçalho da tabela com altura e alinhamento corrigidos;
- fonte da tabela ampliada e colunas reajustadas;
- placar por set com labels mais próximos do placar;
- nomes dos arquivos exportados preservados sem “avelicoach”.
